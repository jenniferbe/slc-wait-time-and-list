<%=


    @studentList = StudentRequest.where(status: "waiting").order('created_at')
    @studentid = 0
    @numTutor = 2
    @studentList.each do |entry|
      if entry.get_wait_position == @numTutor
        @studentid = entry.student_id
        entry.get_wait_position
        break
      end
    end

  @studentList.count
  #@studentid



  %>